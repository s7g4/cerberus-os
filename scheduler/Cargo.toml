[package]
name = "scheduler"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true


[dependencies]
defmt = { version = "1.1.0", optional = true }

[features]
default = ["use-defmt"]
use-defmt = ["dep:defmt"]
