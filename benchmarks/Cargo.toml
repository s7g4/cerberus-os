[package]
name = "benchmarks"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true


[dependencies]
scheduler = { path = "../scheduler" }

[dev-dependencies]
criterion = "=0.8.2"


[[bench]]
name = "scheduler_bench"
harness = false
